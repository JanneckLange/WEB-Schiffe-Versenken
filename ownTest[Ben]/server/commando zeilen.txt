node -v
zeigt die installierte Version an

dann zur server.js navigieren

dann node server.js eingeben und ausführen

dann http://127.0.0.1:3000/ im browser öffnen

dann npm init eingeben (wenn package.json noch nicht existiert)
um den server anzulegen und zu initialisieren

danach packages von npmjs.com installieren


npm install body-parser -s zusätzlich installieren
npm install nach pullen vom repo

package.json (^Versionsnummer heißt ist die mindestversionsnummer)